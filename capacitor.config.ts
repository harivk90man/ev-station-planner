import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.evstationplanner.app',
  appName: 'EV Station Planner',
  webDir: 'out'
};

export default config;
